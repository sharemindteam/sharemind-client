import styled from 'styled-components';

interface ChatStartRequestBoxProps {
 
}
export const ChatStartRequestBox = ({  }: ChatStartRequestBoxProps) => {
  return <Wrapper></Wrapper>;
};

const Wrapper = styled.div``;
